# markitdown-skill Install

## Requirements
- Python 3.8+
- pip

## Steps
1. Install markitdown:
   ```bash
   pip install markitdown
   ```
2. Verify:
   ```bash
   markitdown --version
   ```

## Done
markitdown is ready. Convert any file to Markdown with: `markitdown <filename>`
