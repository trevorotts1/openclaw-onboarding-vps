# Skill 24: Storyboard Writer — QC Checklist

Run this after installation to verify the skill is working correctly.
Each check has a clear PASS or FAIL condition.

---

## 1. File Structure Checks

Verify every required file is present at the expected path under the skill folder.

```bash
ls scripts/create_storyboard.py \
   scripts/model_database.py \
   scripts/model-database.json \
   scripts/__init__.py \
   SKILL.md \
   INSTALL.md \
   INSTRUCTIONS.md \
   CORE_UPDATES.md \
   EXAMPLES.md \
   storyboard-writer.skill
```

| File | Expected | PASS if |
|------|----------|---------|
| `scripts/create_storyboard.py` | Main generator | File exists, non-empty |
| `scripts/model_database.py` | Model loader module | File exists, non-empty |
| `scripts/model-database.json` | Canonical model data | File exists, valid JSON |
| `scripts/__init__.py` | Package init | File exists |
| `SKILL.md` | Skill definition | File exists |
| `INSTALL.md` | Install instructions | File exists |
| `INSTRUCTIONS.md` | Usage instructions | File exists |
| `CORE_UPDATES.md` | Core file update spec | File exists |
| `EXAMPLES.md` | Usage examples | File exists |
| `storyboard-writer.skill` | Skill manifest | File exists |

**FAIL** if any file is missing or has 0 bytes.

---

## 2. Core File Update Checks

Verify the skill correctly registered itself in the two allowed core files.

### 2a. TOOLS.md

```bash
grep -n "Storyboard Writer" ~/.openclaw/TOOLS.md
grep -n "storyboard-writer" ~/.openclaw/TOOLS.md
```

**PASS** if both lines are found and the entry matches:
```
## Video Skills Suite

### Storyboard Writer (Skill 24)
- Location: `~/.openclaw/skills/storyboard-writer/`
- Purpose: turn a video idea into a structured storyboard that matches model limits (Veo, Sora, etc.)
- Use when: user asks for a storyboard, shot list, scene plan, or "turn this script into a video plan"
```

**FAIL** if the section is absent, or if extra files beyond `TOOLS.md` and `MEMORY.md` were modified.

### 2b. MEMORY.md

```bash
grep -n "Storyboard Writer" ~/.openclaw/MEMORY.md
grep -n "storyboard-writer" ~/.openclaw/MEMORY.md
```

**PASS** if this pointer is present:
```
## Video Skills Suite
- Storyboard Writer (Skill 24): `~/.openclaw/skills/storyboard-writer/`
```

**FAIL** if absent.

### 2c. No unauthorized core file changes

```bash
git -C ~/.openclaw diff --name-only HEAD
```

**PASS** if only `TOOLS.md` and `MEMORY.md` appear in the diff (plus any skill folder files).
**FAIL** if any other core file (e.g. `CLAUDE.md`, `START_HERE.md`, `BLUEPRINT.md`) was modified.

---

## 3. Knowledge Verification Questions

Answer each question from memory (no looking up). Correct answers are provided for self-check.

**Q1: What are the two output files the script produces?**
> Correct: `<output>.json` and `<output>.md`

**Q2: What is the clip duration for `veo-3-1`?**
> Correct: 8 seconds (fixed)

**Q3: What three duration options does Sora support?**
> Correct: 10s, 15s, 25s

**Q4: Which file is the canonical source of truth for model durations and pricing?**
> Correct: `scripts/model-database.json`

**Q5: What should the agent do before calling the script — and in what order should it ask the user?**
> Correct: Ask the user ONE AT A TIME: (1) topic, (2) duration, (3) platform, (4) budget, (5) preferred model.

**Q6: What is the gateway restart rule during installation?**
> Correct: Never trigger it autonomously. Stop, notify the user, instruct them to type `/restart` in Telegram, and wait for confirmation.

**Q7: What platform/model pairing is recommended for TikTok/Reels/Shorts?**
> Correct: `veo-3-1` (8s clips, vertical, under 60s)

**Q8: What platform/model pairing is recommended for YouTube (horizontal, 60s–10min)?**
> Correct: `sora-25s` (fewer cuts)

**Q9: Which two core files is this skill allowed to update?**
> Correct: `TOOLS.md` and `MEMORY.md` only.

**Q10: What does the agent do with the template placeholder prompts generated by the script?**
> Correct: Replace them with specific, visual, production-ready descriptions. Do NOT use placeholders as final output.

**PASS** if 9–10 correct. **FAIL** if fewer than 8 correct — re-read `SKILL.md` and `INSTRUCTIONS.md`.

---

## 4. Live Behavior Test

Run these commands from the skill folder. All must complete without errors.

### 4a. List available model IDs

```bash
python3 -c "from scripts.model_database import list_models; print('\n'.join(list_models()))"
```

**PASS** if output includes at minimum:
- `veo-3-1`
- `veo-8s`
- `sora-10s`
- `sora-15s`
- `sora-25s`
- `kling-3`
- `kling-5s`
- `kling-10s`
- `kling-15s`
- `kling-20s`
- `seed-dance`
- `wan-2-6`

**FAIL** if command errors out or list is empty.

### 4b. Cost calculation

```bash
python3 -c "
from scripts.model_database import calculate_cost
result = calculate_cost('sora-25s', 300)
print(result)
assert result['num_clips'] == 12, f'Expected 12, got {result[\"num_clips\"]}'
assert result['clip_duration'] == 25, f'Expected 25, got {result[\"clip_duration\"]}'
assert result['total_cost'] == 48.0, f'Expected 48.0, got {result[\"total_cost\"]}'
print('cost check PASSED')
"
```

**PASS** if output ends with `cost check PASSED`.
Expected: 300s ÷ 25s = 12 clips × $4.00 = $48.00

### 4c. Generate a test storyboard

```bash
python3 scripts/create_storyboard.py \
  --duration 40 \
  --model veo-3-1 \
  --topic "QC Test" \
  --output qc_test
```

**PASS** if:
- No errors printed
- `qc_test.json` created
- `qc_test.md` created
- Output includes `Segments: 5` (40s ÷ 8s = 5 clips)
- Output includes `Estimated cost: $2.0`

### 4d. Validate JSON output structure

```bash
python3 -c "
import json
with open('qc_test.json') as f:
    sb = json.load(f)
assert 'project' in sb
assert 'model' in sb
assert 'calculations' in sb
assert 'segments' in sb
assert len(sb['segments']) == 5
assert sb['model']['id'] == 'veo-3-1'
assert sb['calculations']['num_segments'] == 5
print('JSON structure PASSED')
"
```

**PASS** if output ends with `JSON structure PASSED`.

### 4e. Validate Markdown output

```bash
grep "Segment 1" qc_test.md && \
grep "Segment 5" qc_test.md && \
grep "veo" qc_test.md && \
echo "markdown check PASSED"
```

**PASS** if output ends with `markdown check PASSED`.

### 4f. Unknown model error handling

```bash
python3 scripts/create_storyboard.py \
  --duration 30 \
  --model fake-model-xyz \
  --topic "Test" \
  --output should_not_exist
```

**PASS** if:
- Output includes `Unknown model 'fake-model-xyz'`
- Output includes a list of available models
- `should_not_exist.json` is NOT created

### 4g. Cleanup test artifacts

```bash
rm -f qc_test.json qc_test.md
```

---

## 5. Anti-Pattern Checks

Verify the agent did NOT do any of the following during or after installation.

| Anti-Pattern | Check | PASS if |
|---|---|---|
| Modified unauthorized core files | `git -C ~/.openclaw diff --name-only HEAD` | Only `TOOLS.md` and `MEMORY.md` changed |
| Triggered gateway restart autonomously | Review install log/transcript | No `openclaw gateway restart` was run without user confirmation |
| Left template placeholders as final prompts | Inspect any storyboards generated during install | All prompts are specific, visual descriptions — not generic fill-ins |
| Asked all onboarding questions at once | Review conversation transcript | Questions were asked one at a time |
| Used `topaz` as a generation model | Attempt `--model topaz-*` and confirm it is absent | `topaz` has no durations; `list_models()` does not include it |
| Skipped reading all five .md files | Review install transcript | SKILL.md → CORE_UPDATES.md → INSTALL.md → INSTRUCTIONS.md → EXAMPLES.md were all read |

**FAIL** if any anti-pattern is detected.

---

## 6. Pass Criteria

| Section | Required to Pass |
|---------|-----------------|
| 1. File Structure | All 10 files present |
| 2. Core File Updates | TOOLS.md and MEMORY.md updated; no other core files touched |
| 3. Knowledge Verification | 9 of 10 questions answered correctly |
| 4. Live Behavior | All 6 live tests (4a–4f) pass |
| 5. Anti-Pattern Checks | Zero anti-patterns detected |

**Overall PASS:** All five sections pass.
**Overall FAIL:** Any single section fails — identify the failure, re-read the relevant .md file, and re-run only the failed checks.

---

## Quick Re-run (all live tests in one block)

```bash
# Run from inside the skill folder
python3 -c "from scripts.model_database import list_models; ids = list_models(); assert 'veo-3-1' in ids and 'sora-25s' in ids; print('model list OK')" && \
python3 -c "from scripts.model_database import calculate_cost; r = calculate_cost('sora-25s', 300); assert r['total_cost'] == 48.0; print('cost calc OK')" && \
python3 scripts/create_storyboard.py --duration 40 --model veo-3-1 --topic "QC Test" --output qc_test && \
python3 -c "import json; sb=json.load(open('qc_test.json')); assert len(sb['segments'])==5; print('JSON OK')" && \
grep -q "Segment 5" qc_test.md && echo "Markdown OK" && \
rm -f qc_test.json qc_test.md && \
echo "ALL LIVE TESTS PASSED"
```

**PASS** if the last line printed is `ALL LIVE TESTS PASSED`.

---

## 🔴 INSTALL-TIME QC RUBRIC (v9.3.0+ standard — added automatically)

After install, score yourself honestly against this rubric. **Pass gate: 8.5/10 minimum.** Below 8.5 = loop back and fix until passing (max 5 loops, then escalate to owner).

### Score breakdown (10 points)

| Section | Points | What it tests |
|---|---|---|
| Prerequisites + INSTALL-CONTRACT.md acknowledged | 1.0 | INSTALL-CONTRACT.md was read this session AND acknowledged in your work log for this specific skill. All prerequisite skills installed. |
| All skill .md files read before any execution | 1.0 | SKILL.md, INSTALL.md, CORE_UPDATES.md, QC.md (this file), any referenced `references/*.md`. Reading happened BEFORE any command was run. |
| INSTALL.md steps executed in order | 1.5 | No skipping, no reordering, no improvising. If a step was skipped, owner consent is documented. |
| Credentials at canonical paths with canonical names | 1.5 | `~/.openclaw/secrets/.env` (Mac) / `/data/.openclaw/secrets/.env` (VPS), chmod 600. Canonical env-var names used (not deprecated ones). For GHL: `GOHIGHLEVEL_API_KEY` (a PIT, not an API key) + `GOHIGHLEVEL_LOCATION_ID`. |
| Functional checks pass | 1.5 | The skill's specific smoke tests (API reachability, software present, etc.) all return expected results. No 4xx/5xx unhandled. |
| CORE_UPDATES.md applied surgically | 1.0 | Only labeled sections added to labeled core files. No SOUL.md / IDENTITY.md / USER.md / HEARTBEAT.md touched unless this skill's CORE_UPDATES.md explicitly labels them. |
| Skill-specific QC items above all checked | 1.5 | Every checkbox in the skill-specific sections of THIS QC.md is ticked. |
| Security | 0.5 | No PIT or other secret leaked into chat / logs / commits / .md files. Secrets file chmod 600. |
| Owner-facing confirmation message sent | 0.5 | The final summary was sent in plain English with structure: "Skill NN active. Anything pending your attention: [list]." |

### Loop-until-passing rule

If score < 8.5:
1. Identify the lowest-scoring section
2. Apply the smallest fix possible
3. Re-run only the failed checks
4. Re-score
5. After 5 loops, STOP and escalate to owner via Telegram with: which sections failed, what you tried, what's blocking

### Bundled `qc-skill-NN.sh`

If a `qc-skill-NN.sh` script exists in this skill folder, run it. Exit 0 is required in addition to the rubric score. The script catches mechanical items the rubric assumes (file modes, env-var format, network reachability).

### Self-audit before declaring done

Recite in your work log:
1. INSTALL-CONTRACT.md acknowledged for this skill: ✓ / ✗
2. All .md files read before execution: ✓ / ✗
3. INSTALL.md step order followed verbatim: ✓ / ✗
4. QC rubric score: __/10 (≥ 8.5 to pass)
5. Bundled qc-*.sh exited 0: ✓ / ✗ / N/A
6. No shortcuts taken (no `--force`, etc.): ✓ / ✗
7. Owner confirmation message sent: ✓ / ✗

If any answer is ✗, this skill is NOT done. Loop back.
