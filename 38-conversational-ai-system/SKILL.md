# Skill 38: Conversational AI System (packages playbook v6.0)

## MANDATORY - Teach Yourself Protocol (TYP)

**Before using this skill, complete the Teach Yourself Protocol (Skill 01) on this folder.**

Required read order:
1. SKILL.md (this file)
2. INSTALL.md — one-time setup: prerequisites + the numbered install scripts (`00`–`23`) + QC linters in `scripts/`
3. INSTRUCTIONS.md — the v6.0 playbook organized by Phase 0 through Phase 7 (the operator's runtime walkthrough)
4. CORE_UPDATES.md — what gets appended to AGENTS.md + MEMORY.md
5. references/conversational-ai-strategic-roadmap.md — strategic context (what's ✅ shipped vs 📋 pending)
6. CHANGELOG.md — change history

Per N3 ("read before act"), do not skip. Per N4, follow steps in declared order.


## Governing protocol (binding for this skill and all skills in the repo)

This skill is governed by `../QC-PROTOCOL.md` (repo root) — the Sub-Agent Handoff and Mandatory QC Protocol. Every install, every PR, every multi-file change runs the 10-category QC rubric (8.5 threshold) BEFORE declaring done. Sub-agents receive full instructions (never summaries). See `QC-PROTOCOL.md` Part 5 for the sub-agent contract.

## What This Skill Is

**Skill 38 is the conversational AI BRAIN that runs on top of skill 29 (GHL Convert and Flow).** Skill 29 installs Convert and Flow and the basic GHL integration. Skill 38 adds the brain layer: sales best practices, intelligent follow-up, dual-mode customer service + support, typed knowledge bases, intelligent routing, weekly + monthly self-tuning, model version freshness checking, and the rest of the 32 protocols from the v6.0 playbook (see the SELF-COUNTS note under "What This Skill Ships").

These two skills are SIBLINGS, not duplicates. **Skill 29 is a hard prerequisite.**

## Upstream trigger — Skill 23 (AI Workforce Blueprint) hands off to this skill

When **Skill 23** builds an AI workforce that includes a **Communications, Sales, or Customer-Support**
department, its post-build closeout is REQUIRED to hand off here to scaffold the matching comms
automations. This is an ENFORCED cross-skill chain (not prose): Skill 23 tracks it via the
`commsAutomationStatus` field in its `build-state-schema.json` and re-fires a `[COMMS-AUTOMATION-RESUME]`
self-ping (in `resume-workforce-build.sh`) until this skill has registered at least the appointment-booking
starter playbook + its Build-with-AI prompt (THE TRINITY), verifiable via
`scripts/qc-trinity-registry.sh`. So when you're invoked off a `[COMMS-AUTOMATION-RESUME]` message, the
upstream is Skill 23: read its `INSTRUCTIONS.md → "Moment 3.8"`, then build the trinity here. See
`23-ai-workforce-blueprint/SKILL.md → "Cross-skill chain → Skill 38"`.

## Prerequisites (ALL required; install scripts check at runtime)

- Skill 05 — GHL Setup
- Skill 10 — GitHub Setup (latest version)
- Skill 19 — Humanizer (used ALWAYS-ON; skill 38 does NOT duplicate)
- Skill 29 — GHL Convert and Flow (Convert and Flow CONNECTED to operator's GHL location)

## What This Skill Ships

<!-- SELF-COUNTS: re-verify on EVERY version bump — see scripts/bump-version.sh "Skill 38 self-count
     re-verification" note. Counts as of v1.5.12 (Round-2 backlog F18 Webhook Chaining; v1.5.11 was Round-2 backlog F14 Voice/Phone Integration; v1.5.10 was Round-2 backlog F16 A/B Testing of Reply Variants; v1.5.9 was Round-2 backlog F15 Proactive Outreach Campaigns; v1.5.8 was Round-2 backlog F17 Customer Segmentation Awareness; v1.5.7 was
     F21 Multi-Tenant Agent Isolation; v1.5.6 the ZHC Tag-Prefix Rule QC fix; v1.5.5 the F46 CRM-field QC
     deep-fix): protocols/=45 (*.md — v1.5.12 added webhook-chaining-protocol.md for F18; v1.5.11 added voice-phone-protocol.md for F14; v1.5.10 added ab-testing-protocol.md for F16; v1.5.9 added proactive-outreach-protocol.md for F15; v1.5.8 added customer-segmentation-protocol.md for F17; v1.5.7 added multi-tenant-isolation-protocol.md for F21), scripts/=68 (*.sh — v1.5.12 added qc-webhook-chaining.sh + qc-webhook-chaining.test.sh for F18; v1.5.11 added 30-voice-phone-setup-wizard.sh + qc-voice-phone.sh + qc-voice-phone.test.sh for F14; v1.5.10 added qc-ab-testing.sh + qc-ab-testing.test.sh for F16; v1.5.9 added qc-proactive-outreach.sh + qc-proactive-outreach.test.sh for F15; v1.5.8 added qc-segmentation.sh + qc-segmentation.test.sh for F17; v1.5.7 added qc-multi-tenant.sh + qc-multi-tenant.test.sh for F21; v1.5.4 added
     qc-f45-f47-substance.sh), references/=20 (*.md — v1.5.6 added tag-migration-notes.md for the ZHC
     Tag-Prefix Rule), journey templates=8 dirs.
     Run: ls -1 protocols/*.md scripts/*.sh references/*.md | per-dir wc -l. -->
- 45 protocol files under `protocols/` (humanizer is intentionally NOT here — skill 19 owns it; v1.5.12 added `webhook-chaining-protocol.md` (F18 — Webhook Chaining, OFF by default, the fire-after-a-completed-action OUTBOUND post-action step, INSTRUCTIONS Step 9.49); v1.5.11 added `voice-phone-protocol.md` (F14 — Voice/Phone Integration, OFF by default, a SEPARATE CHANNEL PIPELINE, INSTRUCTIONS Step 9.48; scaffold + setup wizard + honest live-telephony gap); v1.5.10 added `ab-testing-protocol.md` (F16 — A/B Testing of Reply Variants, OFF by default, INSTRUCTIONS Step 9.47); v1.5.9 added `proactive-outreach-protocol.md` (F15 — Proactive Outreach Campaigns, OFF by default, INSTRUCTIONS Step 9.46); v1.5.8 added `customer-segmentation-protocol.md` (F17 — Customer Segmentation Awareness, OFF by default, INSTRUCTIONS Step 9.45); v1.5.7 added `multi-tenant-isolation-protocol.md` (F21 — Multi-Tenant Agent Isolation, the AGENCY tier, OFF by default, INSTRUCTIONS Step 9.44); v1.5.0 added the Round-3 Queue-A six: `zhc-tag-prefix-protocol.md`, `aggression-detection-protocol.md` (F50 — EXTENDS the Safeguard family, bot detection NOT rebuilt), `smart-playbook-switching-protocol.md` (F44 — DETOUR-AND-RETURN, DISTINCT from F33 route-and-stay), `geo-qualification-protocol.md` (F45), `crm-field-write-protocol.md` (F46), `smart-faq-tool-protocol.md` (F47); v1.5.2 adds `zhc-pixel-protocol.md` (F49 — per-client visitor-signal pixel + Pixel Concierge, INSTRUCTIONS Step 9.43))
- **8 customer journey templates** under `templates/journey-templates/` (coach + all 7 verticals fully detailed: consulting, course-creator, e-commerce, real-estate, saas, service-provider, wellness)
- **68 scripts** under `scripts/` (idempotent, OS-aware: Darwin + Linux) — the numbered install scripts `00`–`30` (including the **F14 Voice/Phone** setup wizard `30-voice-phone-setup-wizard.sh` (captures the Twilio creds/number + STT/TTS provider choice into the environment, writes the default-OFF config, and provisions the media-stream bridge + Twilio voice webhook behind the existing Cloudflare tunnel — PAUSING with an exact-needs message if a credential is missing, never flipping the toggle ON itself; live telephony is provisioned at setup, NOT faked) and the **F49 ZHC Pixel** quartet `26-verify-pixel-prerequisites.sh` (CF scope precheck — Pages:Edit + Workers Scripts:Edit + Workers Routes:Edit — HALTs + points at the token Google Doc; records `ZHC_PIXEL_SCOPES_OK`), `27-render-pixel-js.sh` (renders the per-client pixel JS from `templates/zhc-pixel/zhc-pixel.template.js`, substituting the three placeholders), `28-configure-pixel-hook.sh` (registers the `pixel-visitor-signal` hook — `deliver:false`, bot-gate-first `messageTemplate`, the scoped **Pixel Concierge** agent in `agents.list` + the `hook:pixel:` allow-list — and ensures the F52 `pixel-events/` dir; `openclaw config validate`), and `29-deploy-pixel-cloudflare.sh` (GATED on the precheck: `pixel.<CLIENT_DOMAIN>` tunnel hostname + CNAME, CF Pages project + JS deploy, optional Worker + Workers Route; no silent failure) — full protocol in `protocols/zhc-pixel-protocol.md` (Step 9.43); and `25-seed-round3-feature-files.sh`, which idempotently seeds the Round-3 Queue-A data files — `KnowledgeBases/sales/service-areas.md` (F45), `KnowledgeBases/business/faqs.md` (F47), `crm-field-mappings.md` (F46), and the five F52 JSONL log sinks — never overwriting operator content; and `22-notify-client-doc.sh`, the MANDATORY GATED Telegram doc-delivery step: finds the client's chat id by GREPPING THE TRANSCRIPTS `agents/*/sessions/*.jsonl`, sends the doc LINK via the OpenClaw gateway, records `clientDocDelivered` — FLAGS LOUDLY + non-zero on no-chat/send-failure, never a silent skip; `24-self-test-hook.sh`, the AI BACKEND SELF-TEST — the agent drives a synthetic 23-key GHL inbound through its OWN public hook and verifies hook 200 + configured model with no 401/429 + read-the-log + GHL send returned a messageId, with a throwaway test contact created+deleted, BEFORE the client is ever told to test; blocking gate; and `24-update-tools-md.sh`, which preloads the CLIENT `TOOLS.md` with the verified, concise GHL Convert-and-Flow API quick-reference — exact method/URL/3 headers/body/scope per messaging type + calendar + appointment + invoice op — so the agent has the canonical request shapes in core context and answers fast at runtime; idempotent, placeholders-only/zero personal data, source of truth `references/ghl-api-quick-reference.md`) plus `skill38-calendar-sync.sh`, the twenty-three QC linters `qc-23-key-bodies.sh` (machine-enforces the 23-key GHL body rule) + `qc-trinity-registry.sh` (machine-enforces THE TRINITY) + `qc-send-directive.sh` (machine-enforces the MANDATORY GHL send-directive — SEND, not just draft) + `qc-conversation-memory.sh` (machine-enforces the conversation-MEMORY read-before/append-after — hook sessions are single-turn, the per-contact log IS the memory) + `qc-playbook-doc.sh` (machine-enforces the per-playbook human-facing DOC deliverable — Notion → Google Docs → text, URL recorded; a skipped doc fail-closes hand-off) + `qc-notify-client-doc.sh` (machine-enforces the MANDATORY Telegram doc-delivery — the client always gets their link, transcript-grep-based + gated + wired) + `qc-backend-ready.sh` (machine-enforces the backend is ready to RECEIVE — hooks.mappings live + deliver:false + a working model + healthz 200 — before testing/hand-off) + `qc-self-test.sh` (machine-enforces the AI BACKEND SELF-TEST standard — `24-self-test-hook.sh` exists/parses, POSTs the synthetic flat-23-key inbound with the real Bearer, verifies hook 200/{ok:true} + no 401/429 + conversation-log read + GHL send + temp-contact cleanup, documents PASS/FAIL, and is wired as a blocking readiness gate + documented) + `qc-reference-sheet.sh` (machine-enforces that the generated client reference sheet leads with a literal **🚀 Quick Start** section followed by a full **Reference & explanation** section, with each copyable value in its OWN code block — the **Authorization** key and the `Bearer <token>` value in SEPARATE blocks (the value block holds ONLY `Bearer <token>` — it must NOT contain the word "Authorization", and a combined `Authorization: Bearer` block FAILs), the **Content-Type** key and `application/json` value in SEPARATE blocks — plus the copyable ```json GHL Raw Body, the hook URL, the manual Custom-Webhook fill instructions, the **create-tag-FIRST + Settings → Tags** instruction, the **post-build verification** section (Trigger / Custom Webhook / Publish, including the blank-tag-in-a-filter gotcha), AND the enriched **Your Communication Playbooks** section (where playbooks live — `conversation-workflows/` mirrored to Notion — the **"Want another communication playbook? Just ask me!"** CTA with a concrete copyable example, the build walkthrough — brainstorm not a 50-question form → create → store → wire the Workflow-AI prompt to **Convert and Flow** — and the Convert-and-Flow abilities: **create tags / update calendar / book appointments**, plus the explicit "you have an AI connected to your Convert and Flow account — just ask" line, AND the new-playbook CREATION experience — the personal **🔑 trigger word** explained voice-assistant style (Alexa / Hey Siri), the **🤝 "I Do / You Do"** process + the **⏱️ ~15-30 minute** expectation, and the **🧠 brainstorm "things to think about"** list + the "if you're unsure, that's what I'm here to brainstorm" reassurance) — drives the generator offline and FAILs if any is missing) + `qc-config-schema-safety.sh` (machine-enforces that no install script writes a config shape that fails `openclaw config validate` on 2026.5.27 — no `.cron.jobs`, no `agents.defaults.async/.batch`, no jq `//= …;` — and FAILs on any reintroduction) + `qc-no-personal-data.sh` (UNIVERSAL-skill guard — scans the whole tree AND the generated reference sheet and FAILs on ANY real name / client data / real hostname / real token / real Telegram id; negative-tested) + `qc-tools-md-ghl-ref.sh` (machine-enforces the GHL API quick-reference preloaded into the client `TOOLS.md` — FAILs if the block is missing any messaging type / calendar / appointment / invoice op or its scope, exceeds the concise size budget, or leaks any personal/client identifier; runs in SOURCE mode in CI and LIVE mode against an installed `TOOLS.md`) + `qc-zhc-tag-prefix.sh` (machine-enforces the ZHC- programmatic tag-prefix rule — the rule is documented + not-retroactive, the canonical F44/F45/F47/F50 tag tokens are the ZHC- forms, and no bare create-tag example literal survives un-prefixed; negative-tested) + `qc-feature-logs.sh` (machine-enforces the F52 JSONL data contract — each of the five Round-3 feature logs is JSONL with timestamp+event_type, documented in its protocol AND INSTRUCTIONS.md, and seeded by `25-seed-round3-feature-files.sh`; negative-tested) + `qc-f45-f47-substance.sh` (machine-enforces the BEHAVIORAL substance of F45 geo-qualification + F47 smart FAQ from the protocol files — F47: parallel layer alongside the workflow, a SENTENCE not a sub-flow, the "By the way…Coming back to" handoff, sales-vs-ops `faq-scope.md`, `faqs.md` match, the explicit F44-vs-F47 difference, the `ZHC-faq-detoured` hand-off for bigger questions, `ZHC-faq-answered`, `faq-detour-log.jsonl`; F45: default-OFF + the per-product toggle, pixel/IP→area-code→form→ask priority, ALWAYS-confirm + the exact confirmation question + all 5 branches (here/elsewhere/vacation/moving/no-engagement=do-not-disqualify), the 4 out-of-area modes, `service-areas.md` (radius/zips/states/counties), the 3 ZHC tags, and the `geo-qualification-log.jsonl` confirmed_with_customer invariant; negative-tested) + `qc-zhc-pixel.sh` (machine-enforces the F49 ZHC Pixel invariants — the pixel JS template + 3 placeholders + generator, the `pixel-visitor-signal` hook (deliver:false, bot-gate-first, scoped Pixel Concierge + `hook:pixel:` allow-list), the `STEP_1_45_PIXEL_CONCIERGE` AGENTS block + protocol, the ZHC-/ZHC_ pixel tag+field set, the GDPR/CCPA/DNT/deletion privacy controls in BOTH protocol and template, and the scope precheck + gated deploy; delegates the no-personal-data scan to `qc-no-personal-data.sh`; negative-tested via `qc-zhc-pixel.test.sh`) + the **three QC-enforced-standards gates** `qc-communications-playbook-standard.sh` (machine-enforces the Communications Playbook Standard §0 mandatory checklist — channel/persona, opening, goal, SEND rule, conversation-memory, escalation/honesty, quiet-hours/compliance, ZHC- tag prefix, per-channel formatting, the 8 channels; negative-tested) + `qc-ghl-raw-body-standard.sh` (machine-enforces the GHL Raw Body JSON Standard — the FLAT 23-key rule + the exact 23 keys — and composes `qc-23-key-bodies.sh`; negative-tested) + `qc-notion-doc-standard.sh` (machine-enforces the Notion Client-Doc Standard ordered mandatory list and composes `qc-reference-sheet.sh`; negative-tested), + `qc-multi-tenant.sh` (machine-enforces the F21 Multi-Tenant Agent Isolation invariants — the protocol carries the opaque `tenant_id`, the `hooks.mappings` `tenant_id` routing convention, the per-tenant root `tenants/<tenant_id>/` scoping ALL FOUR surfaces (conversation logs / Knowledge Sources / Communication Playbooks / Conversation Workflows), the `tenant.md` config directive, the `ZHC-<tenant_id>-` tag namespace, the operator-only/never-customer-invoked guard, the "Client A never leaks to Client B" isolation invariant, the honest "NOT a runtime DB migration" scope; AGENTS Step 0.8 + MEMORY Rule 26; the PII-free `multi-tenant-events.jsonl` contract seeded by script 25 + the per-tenant root scaffold; toggle default OFF; negative-tested via `qc-multi-tenant.test.sh`), + `qc-segmentation.sh` (machine-enforces the F17 Customer Segmentation Awareness invariants — the protocol carries the five canonical segments (vip/prospect/returning/at-risk/churned), the per-client GHL-tag → segment mapping (`tag_map` + `segment-map.md`), the multi-tag precedence (at-risk > vip > churned > returning > prospect), ALL FOUR behavior overrides (response priority / the F4-Step 9.6 sentiment-escalation threshold / the Communication Playbook tier / the Step 9.11 confidence threshold), the BEFORE-reply-draft Step 1.85 placement, the `ZHC-segment-` tag prefix, the operator-only/never-customer-invoked self-promotion guard, and the honest "READS membership / NOT a new CRM or scoring engine" scope; AGENTS Step 1.85 + MEMORY Rule 27; the PII-free `segmentation-events.jsonl` contract seeded by script 25 + the `segment-map.md` companion; toggle default OFF; negative-tested via `qc-segmentation.test.sh`), + `qc-proactive-outreach.sh` (machine-enforces the F15 Proactive Outreach Campaigns invariants — the protocol carries the campaign-definitions dir (`outreach-campaigns/`), BOTH trigger kinds (time-based cron + event-based), the GHL-tag audience filter, the message rendered THROUGH the Communication Playbook (same brand voice as a reactive reply), the frequency cap, opt-out respect (`ZHC-outreach-opted-out`, global), the `ZHC-outreach-<campaign>` tag, the STRICT quiet-hours respect (Step 9.8 — a quiet-window touch queues/defers), the reactive-vs-proactive separation (`direction:proactive` on every log line), the operator-only/never-customer-invoked SEND guard (outbound-injection), the F29 Intelligent-Follow-up migration relationship, and the honest "reuses GHL send + openclaw cron + Communication Playbooks / NOT a new sending service, scheduler, or CRM" scope; the engine is CRON/EVENT-DRIVEN so there is intentionally NO inbound AGENTS step (no `STEP_9_46` marker — the cron+event wiring is documented in the protocol) + MEMORY Rule 28; the PII-free `outreach-events.jsonl` contract (`campaign_fired`) seeded by script 25 + the `outreach-campaigns/` dir + the example campaign; toggle default OFF; negative-tested via `qc-proactive-outreach.test.sh`), + `qc-ab-testing.sh` (machine-enforces the F16 A/B Testing of Reply Variants invariants — the protocol carries TWO Communication-Playbook variants (a/b) per channel, the per-channel experiment definition (`experiments` + `ab-experiments/<channel>.md`), the DETERMINISTIC-BY-CONTACT assignment (a contact stays in one arm — sticky), the AT-DRAFT-TIME Step 1.87 placement (after segmentation Step 1.85, before the reply draft Step 1.9), ALL THREE outcome metrics (booked / converted / sentiment trajectory), the documented two-proportion z-test + the sane default N (30/arm), the auto-promote-with-operator-notify flow, the `ZHC-abtest-variant-` tag prefix, the operator-only/never-customer-invoked A/B-injection guard, and the honest "REUSES the reply-draft path + the existing booking/conversion/sentiment signals + the Communication Playbooks / NOT a new statistics engine, experimentation platform, or CRM" scope; AGENTS Step 1.87 `STEP_1_87_AB_TESTING` + MEMORY Rule 29; the PII-free `ab-test-events.jsonl` contract (`variant_assigned` / `outcome_recorded` / `experiment_decided`) seeded by script 25 + the `ab-experiments/` scaffold (the example experiment + the two variant overlays); toggle default OFF; negative-tested via `qc-ab-testing.test.sh`), + `qc-voice-phone.sh` (machine-enforces the F14 Voice/Phone Integration invariants — the protocol carries the STT Whisper-large-v3 (via OpenRouter/Groq/local Ollama) → brain → TTS (ElevenLabs Flash 2.5 / OSS) pipeline, the Twilio Voice + Media Streams (SIP/PSTN) transport, the inbound `/hooks/voice-call-event` hook scaffolding, the call-lifecycle state machine (greeting→listen→respond→handoff/booking), the < 800ms first-audio latency target, the degrade-to-text fallback, the setup wizard, the operator-only/never-customer-invoked OUTBOUND-dial guard, the `ZHC-voice-inbound`/`ZHC-voice-outbound` tags, and the HONEST live-telephony gap (NOT faked — live telephony needs operator-provisioned Twilio/STT/TTS creds + the media-stream bridge provisioned at setup); MEMORY Rule 30 + the setup wizard `30-voice-phone-setup-wizard.sh`; voice is a SEPARATE CHANNEL PIPELINE so its lifecycle + hook live in the AGENTS `VOICE_PHONE_PIPELINE` marker, NOT a Step 9.x reply-draft block; the PII-free `voice-call-events.jsonl` contract (`voice_call`) seeded by script 25; toggle default OFF; negative-tested via `qc-voice-phone.test.sh`), + `qc-webhook-chaining.sh` (machine-enforces the F18 Webhook Chaining invariants — the protocol carries the OUTBOUND post-action front-door framing, the operator-defined `webhook-chains/` registry, the FOUR allow-listed trigger events (`booking_completed`/`invoice_sent`/`escalation_raised`/`transcript_exported`, any other ignored), the `https://`-only target, the PII-free payload (opaque `<CONTACT_REF>` + action ids, never a name/email/phone/address or the transcript body), the retry policy (exponential backoff + `max_attempts`), the async/never-block-the-reply invariant, the operator-only/never-customer-invoked OUTBOUND guard (SSRF/exfiltration — a customer can never name/add/trigger a target), and the `ZHC-webhook-chain-fired`/`ZHC-webhook-chain-failed` tags; AGENTS Step 2.9 `STEP_2_9_WEBHOOK_CHAINING` + MEMORY Rule 31; the PII-free `webhook-chain-events.jsonl` contract (`webhook_chain`) seeded by script 25 + the `webhook-chains/` registry + the example chain; toggle default OFF; negative-tested via `qc-webhook-chaining.test.sh`), and the `qc-trinity-registry.test.sh` + `qc-playbook-doc.test.sh` + `qc-zhc-pixel.test.sh` + `qc-multi-tenant.test.sh` + `qc-segmentation.test.sh` + `qc-proactive-outreach.test.sh` + `qc-ab-testing.test.sh` + `qc-voice-phone.test.sh` + `qc-webhook-chaining.test.sh` fixture suites (prove registry reconciliation + the doc gate + the F49 pixel gate + the F21 multi-tenant gate + the F17 segmentation gate + the F15 proactive-outreach gate + the F16 A/B-testing gate + the F14 voice/phone gate + the F18 webhook-chaining gate)
- 20 reference documents under `references/` (deep-dives + the full v6.0 source playbook + **tag-migration-notes.md** — the optional, operator-driven legacy-tag → `ZHC-` migration reference for the ZHC Tag-Prefix Rule — + the **four QC-enforced standards** — `workflow-ai-instructions-standard.md` (the Build-with-AI prompt), `communications-playbook-standard.md` (the per-channel/per-scenario voice + behavior, with a binding §0 "EVERY COMMUNICATION PLAYBOOK MUST INCLUDE ALL OF THE FOLLOWING" checklist, machine-enforced by `scripts/qc-communications-playbook-standard.sh`), `ghl-raw-body-json-standard.md` (the FLAT 23-key GHL Custom Webhook RAW BODY, machine-enforced by `scripts/qc-ghl-raw-body-standard.sh` composing `qc-23-key-bodies.sh`), and `notion-client-doc-standard.md` (the ordered client Quick-Start Notion setup-doc structure, machine-enforced by `scripts/qc-notion-doc-standard.sh` composing `qc-reference-sheet.sh`) + the **Cloudflare & GoDaddy Setup Guide** from School of AI — the client-facing walk-through for the missing-CF-token halt path — plus **VPS-VS-MAC-INSTALL.md**, the VPS-vs-Mac install-considerations reference mirrored into the generated client doc, and **ghl-api-quick-reference.md**, the concise verified GHL Convert-and-Flow API request-shape block preloaded into the client `TOOLS.md` by `scripts/24-update-tools-md.sh`)
- **AGENTS.md updates:** Steps 1.7, 1.8, 1.9, 2.8 inserted; Step 1.75 upgraded; v1.5.0 adds Step 1.35 (F50 aggression, PRE-routing), Step 2.0 (F44 interrupts + F47 inline FAQ), Step 2.5 (F45 geo-qualification), and the ZHC-/ZHC_ tag+field-namespace note; v1.5.2 adds Step 1.45 (F49 Pixel Concierge — scoped to `hook:pixel:*` sessions; marker `STEP_1_45_PIXEL_CONCIERGE`); v1.5.7 adds Step 0.8 (F21 Multi-Tenant Agent Isolation — resolve the active tenant FIRST; marker `STEP_0_8_MULTI_TENANT_ISOLATION`); v1.5.8 adds Step 1.85 (F17 Customer Segmentation Awareness — segment lookup BEFORE the reply draft; marker `STEP_1_85_SEGMENTATION_AWARENESS`, coexisting with the operator-side Workflow-Builder triggers in the same 1.85 region) (all via `scripts/05-update-agents-md.sh` marker blocks); **v1.5.9 adds NO AGENTS.md step** — F15 Proactive Outreach Campaigns is cron/event-driven (not invoked on a customer message), so its cron + event hooks are documented in `protocols/proactive-outreach-protocol.md` instead of an AGENTS.md marker block; v1.5.10 adds Step 1.87 (F16 A/B Testing of Reply Variants — variant selection AT DRAFT TIME, after segmentation Step 1.85, before the reply draft Step 1.9; marker `STEP_1_87_AB_TESTING`, distinct from the 1.85 blocks); **v1.5.11 adds NO numbered AGENTS.md step** — F14 Voice/Phone Integration is a SEPARATE CHANNEL PIPELINE (not a text reply-draft step), so it adds the marker block `VOICE_PHONE_PIPELINE` documenting the call lifecycle (greeting→listen→respond→handoff/booking) + the `/hooks/voice-call-event` hook instead of a Step 9.x block; **v1.5.12 adds Step 2.9** (F18 Webhook Chaining — the fire-after-a-completed-action POST-ACTION step, after the CRM-field-write Step 2.5 + the runtime routing 2.8; marker `STEP_2_9_WEBHOOK_CHAINING`, the outbound counterpart to the inbound GHL hook, distinct from a reply-draft step)
- **MEMORY.md design rules 6-18** appended (v1.5.0 adds rules 20-24 in a fresh marker block — ZHC tag-prefix, aggression, detour-and-return, geo+CRM-field, smart-FAQ+logging); v1.5.7 adds rule 26 (F21 Multi-Tenant Agent Isolation) in a fresh Round-2 marker block; v1.5.8 adds rule 27 (F17 Customer Segmentation Awareness) in a fresh Round-2 marker block; v1.5.9 adds rule 28 (F15 Proactive Outreach Campaigns) in a fresh Round-2 marker block; v1.5.10 adds rule 29 (F16 A/B Testing of Reply Variants) in a fresh Round-2 marker block; v1.5.11 adds rule 30 (F14 Voice/Phone Integration) in a fresh Round-2 marker block; v1.5.12 adds rule 31 (F18 Webhook Chaining) in a fresh Round-2 marker block
- **4 cron jobs registered:** Sunday 2am tune-up; Saturday 11pm proactive scan + 11:30pm model freshness; 1st-of-month comprehensive review

## What This Skill Does NOT Do

- Does NOT modify skill 17 (self-improving-agent), 18 (proactive-agent), 31 (upgraded-memory-system), 19 (humanizer), or 29 (ghl-convert-and-flow). Each runs independently.
- All six Round-2 backlog features have shipped. (F18 Webhook Chaining shipped v1.5.12 — protocol + registry + retry/backoff + PII-free log + post-action wiring; F14 Voice/Phone Integration shipped v1.5.11 — scaffold + setup wizard + honest live-telephony gap; F16 A/B Testing of Reply Variants shipped v1.5.10; F15 Proactive Outreach Campaigns shipped v1.5.9; F17 Segmentation shipped v1.5.8; F21 Multi-Tenant Isolation shipped v1.5.7.)
- Does NOT auto-update primary models. Model version checks SUGGEST; operator approves.
- Does NOT generate discount codes outside per-product policy.
- Does NOT promise refunds/exceptions without operator approval (honesty floor enforced).

## When the Agent Should Invoke This Skill

After skills 05, 10, 19, 29 are installed AND the operator is ready to wire up the full conversational AI brain. Estimated install time: 60-90 minutes (per the v6.0 playbook).
