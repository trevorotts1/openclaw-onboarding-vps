name: QC static checks

# Wave 5.2 of the post-analysis remediation. The full qc-system-integrity.sh
# needs a live OpenClaw install (~/.openclaw/ or /data/.openclaw/) so it can't
# run in CI as-is. This job runs the static subset — invariants that are
# detectable from the repo alone — so we catch regressions on every push:
#
#   • All Python scripts in shared-utils/ and active skills parse without error
#   • DEFAULT_WEIGHTS in adaptive_weights.py sums to 1.0
#   • company-config.template.json is valid JSON (schema v2.0)
#   • persona-categories.json is valid JSON
#   • No Anthropic model strings in Skill 22/23 scripts
#   • No flat-constant scoring (0.7/0.7/0.8/0.8) lingering in persona-selector-v2.py
#
# Triggers on every push to main and every PR targeting main.

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  qc-static:
    name: QC static invariants
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Python syntax (shared-utils + Skill 22/23 scripts)
        shell: bash
        run: |
          set -euo pipefail
          fail=0
          for f in $(find shared-utils 22-book-to-persona-coaching-leadership-system 23-ai-workforce-blueprint -type f -name '*.py'); do
            python3 -c "import ast; ast.parse(open('$f').read())" || { echo "  ✗ syntax: $f"; fail=1; }
          done
          [ $fail -eq 0 ] && echo "✓ Python syntax OK across all skill scripts"
          exit $fail

      - name: DEFAULT_WEIGHTS sums to 1.0
        shell: python {0}
        run: |
          import sys
          sys.path.insert(0, 'shared-utils')
          from adaptive_weights import DEFAULT_WEIGHTS
          total = sum(DEFAULT_WEIGHTS.values())
          if not (0.999 <= total <= 1.001):
              print(f"✗ DEFAULT_WEIGHTS sums to {total}, expected 1.0")
              sys.exit(1)
          print(f"✓ DEFAULT_WEIGHTS sums to {total}")

      - name: company-config template is valid JSON
        shell: bash
        run: |
          python3 -c "
          import json, sys
          d = json.load(open('23-ai-workforce-blueprint/templates/company-config.template.json'))
          required = ['schema_version', 'mission', 'owner_values', 'company_kpis', 'dept_kpis']
          missing = [k for k in required if k not in d]
          if missing:
              print(f'✗ missing required fields: {missing}'); sys.exit(1)
          if d['schema_version'] != '2.0':
              print(f'✗ schema_version is {d[\"schema_version\"]}, expected 2.0'); sys.exit(1)
          print('✓ company-config template valid (schema v2.0)')
          "

      - name: No Anthropic model strings in Skill 22/23 scripts
        shell: bash
        run: |
          set -uo pipefail
          if grep -rEn "'(claude-[a-z0-9-]+|anthropic/[a-z0-9-]+)'" 22-book-to-persona-coaching-leadership-system/scripts 23-ai-workforce-blueprint/scripts shared-utils/ 2>&1; then
            echo "✗ Found Anthropic model string(s) above"; exit 1
          fi
          echo "✓ No Anthropic model strings in pipeline scripts"

      - name: persona-selector-v2 no longer uses flat scoring constants
        shell: bash
        run: |
          set -uo pipefail
          # Check that _heuristic_layer_scores is structured (reads cc fields) not flat 0.7/0.8.
          # Specifically: should NOT contain `mission_score = 0.8` or `dept_score = 0.7` as bare assignments.
          if grep -E "^\s*(mission_score|values_score|dept_score|company_kpi_score)\s*=\s*0\.[78](\s|$)" 23-ai-workforce-blueprint/scripts/persona-selector-v2.py 2>&1 | grep -v "= 0\.6"; then
            echo "✗ Flat-constant scoring detected in persona-selector-v2.py"; exit 1
          fi
          echo "✓ No flat-constant scoring in persona-selector-v2.py"

      - name: llm_score module loads
        shell: bash
        run: |
          python3 -c "
          import sys
          sys.path.insert(0, 'shared-utils')
          from llm_score import score_layer, summarize_persona_blueprint, _cache_key, NEUTRAL_FALLBACK_SCORE
          assert callable(score_layer)
          assert callable(summarize_persona_blueprint)
          assert NEUTRAL_FALLBACK_SCORE == 0.6
          k = _cache_key('test-persona', 'mission', 'summary', 'context')
          assert len(k) == 64
          print('✓ llm_score module imports + cache key generation OK')
          "

      - name: Skill 38 GHL RAW BODY 23-key rule
        shell: bash
        run: |
          set -uo pipefail
          # Machine-enforce the 23-key flat placeholder-free GHL body rule across
          # every object-A example in references/ + templates/ + scripts/
          # (including references/v6.0-source-playbook.md).
          bash 38-conversational-ai-system/scripts/qc-23-key-bodies.sh
          echo "✓ All Skill 38 GHL RAW BODY examples satisfy the 23-key rule"

      - name: Skill 38 trinity-registry reconciliation (bullet + table fixtures)
        shell: bash
        run: |
          set -uo pipefail
          # Prove qc-trinity-registry.sh reconciles the BULLET registry the
          # installer actually writes (not just the documented table) — catches
          # registered-but-missing-files and file-present-but-unregistered rows.
          bash 38-conversational-ai-system/scripts/qc-trinity-registry.test.sh
          echo "✓ qc-trinity-registry reconciles bullet and table registries"

      - name: Skill 38 MANDATORY GHL send-directive
        shell: bash
        run: |
          set -uo pipefail
          # Machine-enforce that EVERY GHL inbound server-mapping messageTemplate
          # (the installer's canonical template + the playbook/reference examples)
          # carries the mandatory send-directive: SEND via the GHL Conversations
          # API, drafting-is-NOT-sending, do-not-end-turn-until-messageId. Drafting
          # is not sending — a hook without this clause silently drops the reply.
          bash 38-conversational-ai-system/scripts/qc-send-directive.sh
          echo "✓ All Skill 38 GHL server-mapping messageTemplates carry the SEND-directive"

      - name: Skill 38 MANDATORY conversation-memory (read-before + append-after)
        shell: bash
        run: |
          set -uo pipefail
          # Machine-enforce that EVERY GHL inbound server-mapping messageTemplate
          # (installer canonical + playbook/reference examples) READS the contact's
          # conversation log BEFORE replying and APPENDS the inbound+reply AFTER
          # sending. GHL inbound hook sessions are SINGLE-TURN / stateless — the
          # per-contact log IS the agent's only cross-message memory; a template
          # that drops either step makes the agent forget mid-conversation.
          bash 38-conversational-ai-system/scripts/qc-conversation-memory.sh
          echo "✓ All Skill 38 GHL server-mapping messageTemplates READ-before and APPEND-after the conversation log"

      - name: Skill 38 per-playbook human-facing DOC gate (fixtures)
        shell: bash
        run: |
          set -euo pipefail
          # Machine-enforce the per-playbook human-facing DOC deliverable: when a
          # conversation playbook is created, a human-facing copy MUST be created in
          # the CLIENT's account (Notion -> Google Docs -> plain text) and its
          # URL/path RECORDED (registry row or run-manifest playbookDocs[]). On a
          # recent client this step was SKIPPED -- the agent scaffolded the playbook
          # locally and reported "clean" but never created the client's Notion doc.
          # The live gate (qc-playbook-doc.sh) runs against a client's installed
          # conversation-workflows/ folder, so CI proves it via the fixture suite
          # (mirroring qc-trinity-registry.test.sh): a playbook with no recorded doc
          # FAILS; an empty folder is NO_PLAYBOOKS (exit 2), never a blind pass.
          bash 38-conversational-ai-system/scripts/qc-playbook-doc.test.sh
          echo "✓ qc-playbook-doc enforces the per-playbook human-facing doc deliverable"

      - name: Skill 38 client reference sheet copy-paste artifacts
        shell: bash
        run: |
          set -uo pipefail
          # Machine-enforce that the generated CLIENT REFERENCE SHEET is bulletproof:
          # it must LEAD with a literal "🚀 Quick Start" section and STILL carry a
          # full "Reference & explanation" section after it (both, in that order);
          # each copyable value must be in its OWN code block — the Authorization KEY
          # and the "Bearer <token>" VALUE in SEPARATE blocks (never combined), and
          # the Content-Type KEY and "application/json" VALUE in SEPARATE blocks (50+
          # clients copy each field individually); plus the copyable ```json GHL Raw
          # Body, the hook URL, the manual Custom-Webhook fill steps, the
          # create-tag-FIRST + "Settings → Tags" instruction, and the post-build
          # verification section (Trigger / Custom Webhook / Publish, including the
          # blank/non-existent-tag-in-a-"does not contain"-filter gotcha that stranded
          # a live client, Teresa). qc-reference-sheet.sh drives
          # 21-generate-client-reference-sheet.sh in a sandbox (no openclaw on PATH →
          # offline Layer-3 markdown) and FAILS if any marker is missing, so a
          # template/script regression that drops them fails the build. BASH (with an
          # inline grep/awk core), so it respects the .py claude-/anthropic ban.
          bash 38-conversational-ai-system/scripts/qc-reference-sheet.sh
          echo "✓ Skill 38 client reference sheet: 🚀 Quick Start + after-it explanation + separate Authorization/Content-Type key+value blocks + bearer token + copyable Raw Body JSON + hook URL + manual-fill + create-tag-first + post-build verification"

      - name: Skill 38 MANDATORY Telegram doc-delivery (gated, transcript-grep)
        shell: bash
        run: |
          set -uo pipefail
          # Machine-enforce the un-skippable Telegram doc-delivery step. The
          # install is NOT complete until the client has been SENT their
          # Quick-Start / Notion doc LINK via Telegram. qc-notify-client-doc.sh
          # asserts scripts/22-notify-client-doc.sh exists, GREPS THE TRANSCRIPTS
          # (agents/*/sessions/*.jsonl, not sessions.json keys only — the Teresa
          # lesson), is GATED (records clientDocDelivered + exits non-zero on
          # miss/failure — never a silent skip), sends via the OpenClaw gateway
          # (never api.telegram.org direct), and is wired into the QC checklist +
          # INSTRUCTIONS. Also smoke-test the delivery script itself: a fixture
          # transcript must resolve the most-frequent non-operator chat id, and a
          # transcript with only the operator id must FAIL (exit 1) and record
          # clientDocDelivered=false. BASH only (respects the .py claude-/anthropic ban).
          bash 38-conversational-ai-system/scripts/qc-notify-client-doc.sh
          # --- fixture smoke test (success path: client id wins) ---
          tdir="$(mktemp -d)"
          mkdir -p "$tdir/home/.openclaw/agents/main/sessions" "$tdir/master"
          jl="$tdir/home/.openclaw/agents/main/sessions/s1.jsonl"
          # Synthetic ids only (no real person): 1000000001 = the operator (excluded),
          # 2000000002 = the client (the most-frequent non-operator id => should win).
          printf '%s\n' '{"meta":{"chat":{"id":1000000001}},"text":"operator"}' >  "$jl"
          printf '%s\n' '{"meta":{"chat":{"id":2000000002}},"text":"client"}'   >> "$jl"
          printf '%s\n' '{"key":"agent:main:telegram:direct:2000000002"}'        >> "$jl"
          printf '%s\n' '{"from":{"id":2000000002},"chatId":2000000002}'         >> "$jl"
          echo "https://notion.so/demo-abc" > "$tdir/master/.notion-reference-page-url"
          printf '# Run Manifest\n' > "$tdir/master/run-manifest-20260529-000000.md"
          out="$(HOME="$tdir/home" MASTER_FILES_DIR="$tdir/master" OPENCLAW_HOME="$tdir/home/.openclaw" \
            OPERATOR_TELEGRAM_CHAT_ID=1000000001 \
            bash 38-conversational-ai-system/scripts/22-notify-client-doc.sh --dry-run --json)"
          echo "$out" | grep -q '"chat":"2000000002"' || { echo "✗ transcript-grep did not pick the client id"; exit 1; }
          echo "$out" | grep -q '"clientDocDelivered":true' || { echo "✗ dry-run did not record delivered=true"; exit 1; }
          # --- fixture smoke test (failure path: only operator id => exit 1, false) ---
          tdir2="$(mktemp -d)"
          mkdir -p "$tdir2/home/.openclaw/agents/main/sessions" "$tdir2/master"
          printf '{"meta":{"chat":{"id":1000000001}}}\n' > "$tdir2/home/.openclaw/agents/main/sessions/s1.jsonl"
          echo "https://notion.so/demo-xyz" > "$tdir2/master/.notion-reference-page-url"
          printf '# Run Manifest\n' > "$tdir2/master/run-manifest-20260529-000000.md"
          rc=0
          HOME="$tdir2/home" MASTER_FILES_DIR="$tdir2/master" OPENCLAW_HOME="$tdir2/home/.openclaw" \
            OPERATOR_TELEGRAM_CHAT_ID=1000000001 \
            bash 38-conversational-ai-system/scripts/22-notify-client-doc.sh >/dev/null 2>&1 || rc=$?
          [ "$rc" -eq 1 ] || { echo "✗ no-client-chat path should exit 1, got $rc"; exit 1; }
          grep -q 'clientDocDelivered=false' "$tdir2/master/run-manifest-20260529-000000.md" || { echo "✗ no-chat path did not record clientDocDelivered=false"; exit 1; }
          rm -rf "$tdir" "$tdir2"
          echo "✓ Skill 38 Telegram doc-delivery is mandatory, transcript-grep-based, gated, and wired"

      - name: Skill 38 backend-ready gate (no-config => SKIP, not fail)
        shell: bash
        run: |
          set -uo pipefail
          # The backend-ready gate (hooks.mappings live + deliver:false + a working
          # model + healthz 200) is a LIVE check. In CI there is no install, so it
          # must exit 3 (NO_CONFIG) — a SKIP, never a false failure. Assert that.
          rc=0
          bash 38-conversational-ai-system/scripts/qc-backend-ready.sh --config /nonexistent/openclaw.json >/dev/null 2>&1 || rc=$?
          [ "$rc" -eq 3 ] || { echo "✗ backend-ready with no config should exit 3 (SKIP), got $rc"; exit 1; }
          echo "✓ Skill 38 backend-ready gate exits 3 (SKIP) when no install is present"

      - name: Skill 38 config schema-safety (no config-invalidating install scripts)
        shell: bash
        run: |
          set -uo pipefail
          # Machine-enforce that NO Skill 38 install script writes a config shape
          # that fails `openclaw config validate` on 2026.5.27, and that none uses
          # jq syntax jq 1.7 rejects. Catches the three v1.4.11 install-script bugs
          # found on a live box: (1) a legacy `.cron.jobs` write (crons must go
          # through `openclaw cron add`), (2) unsupported `agents.defaults.async/
          # .batch` model keys, (3) a `.hooks //= {};` jq statement (jq 1.7 rejects
          # the top-level `;`). A regression that reintroduces any of them fails CI.
          bash 38-conversational-ai-system/scripts/qc-config-schema-safety.sh
          echo "✓ Skill 38 install scripts are config-schema-safe (no .cron.jobs / agents.defaults.async-batch / jq '//= …;')"

      - name: Skill 38 AI backend self-test wiring (24-self-test-hook.sh)
        shell: bash
        run: |
          set -uo pipefail
          # The agent self-tests the FULL inbound->reply chain by GROUND TRUTH
          # BEFORE the client ever tests: backend prepared -> POST a synthetic
          # 23-key GHL inbound to its OWN public hook (real Bearer token) -> verify
          # hook 200 + configured model (no 401/429) + read the log + GHL
          # Conversations API send returned a messageId (temp test contact created
          # then deleted, test log removed). The live run needs an install; CI runs
          # the STATIC wiring check (script present + documents the full chain +
          # pass/fail + cleanup + wired as a BLOCKING gate in the QC checklist).
          bash 38-conversational-ai-system/scripts/24-self-test-hook.sh --check-wiring
          # The standalone backend-self-test STANDARD gate (static assertions:
          # the self-test script exists/parses, POSTs the synthetic flat-23-key
          # inbound with the real Bearer, verifies the full chain + cleanup, and is
          # wired as a blocking readiness gate + documented).
          bash 38-conversational-ai-system/scripts/qc-self-test.sh
          # Also assert the live mode SKIPs (exit 3) when there is no install.
          rc=0
          bash 38-conversational-ai-system/scripts/24-self-test-hook.sh --live --config /nonexistent/openclaw.json >/dev/null 2>&1 || rc=$?
          [ "$rc" -eq 3 ] || { echo "✗ self-test --live with no config should exit 3 (SKIP), got $rc"; exit 1; }
          echo "✓ Skill 38 AI backend self-test exists, documents the full chain + cleanup, is wired as a blocking gate, and SKIPs cleanly with no install"

      - name: Skill 38 NO personal/client data (universal-skill guard)
        shell: bash
        run: |
          set -uo pipefail
          # Skill 38 is UNIVERSAL — the SAME brain for any client. Real names /
          # client data must NOT appear anywhere in the skill OR in the doc it
          # generates (a client once opened "their" Notion doc and saw someone
          # else's name). qc-no-personal-data.sh scans the whole tree AND drives
          # 21-generate-client-reference-sheet.sh in an offline sandbox to scan the
          # generated markdown, and FAILS on any forbidden identifier.
          bash 38-conversational-ai-system/scripts/qc-no-personal-data.sh
          # Negative self-test: a planted identifier MUST make the gate FAIL (exit 1).
          ng="$(mktemp -d)"
          cp -r 38-conversational-ai-system "$ng/skill"
          printf '\n<!-- planted leak: Teresa / 5252140759 -->\n' >> "$ng/skill/INSTRUCTIONS.md"
          rc=0
          bash "$ng/skill/scripts/qc-no-personal-data.sh" --skill-dir "$ng/skill" >/dev/null 2>&1 || rc=$?
          [ "$rc" -eq 1 ] || { echo "✗ no-personal-data gate should FAIL (exit 1) on a planted identifier, got $rc"; exit 1; }
          rm -rf "$ng"
          echo "✓ Skill 38 carries no personal/client data (tree + generated output), and the guard fails closed on a planted identifier"

      - name: Skill 38 TOOLS.md GHL API quick-reference (source + installer + gate fixtures)
        shell: bash
        run: |
          set -uo pipefail
          # The installer (24-update-tools-md.sh) preloads the CLIENT agent's TOOLS.md
          # with the verified GHL Convert-and-Flow API quick-reference so the agent has
          # the exact request shapes (method/URL/3 headers/body/scope) in its CORE
          # context and answers fast at runtime. The gate (qc-tools-md-ghl-ref.sh)
          # FAILs if the canonical block is missing any messaging type (SMS/Email/FB/
          # IG/Live_Chat), any calendar op (list/get/create/free-slots), any
          # appointment op (book/reschedule/cancel), the send-invoice op, or any
          # required scope; if it exceeds the concise size budget; or if it leaks any
          # personal/client identifier. CI proves all of it statically (no install
          # needed). BASH only (respects the .py claude-/anthropic ban).
          ref=38-conversational-ai-system/references/ghl-api-quick-reference.md
          gate=38-conversational-ai-system/scripts/qc-tools-md-ghl-ref.sh
          inst=38-conversational-ai-system/scripts/24-update-tools-md.sh

          # (1) source block is complete + concise + leak-free
          bash "$gate"

          # (2) installer injects the block, is idempotent, and the injected block passes LIVE
          tdir="$(mktemp -d)"
          tm="$tdir/TOOLS.md"
          printf '# TOOLS.md\n\npre-existing content\n' > "$tm"
          PUBLIC_HOSTNAME="claw.ci-fixture.example.com" bash "$inst" "$tm" >/dev/null
          [ "$(grep -c 'BEGIN SKILL38: GHL_API_QUICK_REFERENCE' "$tm")" -eq 1 ] || { echo "✗ installer did not inject exactly one block"; exit 1; }
          grep -q 'pre-existing content' "$tm" || { echo "✗ installer clobbered existing TOOLS.md content"; exit 1; }
          PUBLIC_HOSTNAME="claw.ci-fixture.example.com" bash "$inst" "$tm" >/dev/null   # idempotent re-run
          [ "$(grep -c 'BEGIN SKILL38: GHL_API_QUICK_REFERENCE' "$tm")" -eq 1 ] || { echo "✗ installer duplicated the block on re-run (not idempotent)"; exit 1; }
          bash "$gate" --tools-md "$tm" >/dev/null || { echo "✗ injected block failed the live gate"; exit 1; }

          # (3) live gate exits 3 (SKIPPED-INCOMPLETE) when the marker is absent
          printf '# TOOLS.md\n\nno block\n' > "$tdir/empty.md"
          rc=0; bash "$gate" --tools-md "$tdir/empty.md" >/dev/null 2>&1 || rc=$?
          [ "$rc" -eq 3 ] || { echo "✗ missing-marker TOOLS.md should exit 3, got $rc"; exit 1; }

          # (4) negative: a leaked email / oversize budget / dropped op each FAIL (exit 1)
          cp "$ref" "$tdir/leak.md"; printf '\nclient@acmeco.com\n' >> "$tdir/leak.md"
          rc=0; bash "$gate" --ref-file "$tdir/leak.md" >/dev/null 2>&1 || rc=$?
          [ "$rc" -eq 1 ] || { echo "✗ email-leak block should FAIL (exit 1), got $rc"; exit 1; }
          rc=0; bash "$gate" --ref-file "$ref" --max-lines 10 >/dev/null 2>&1 || rc=$?
          [ "$rc" -eq 1 ] || { echo "✗ oversize block should FAIL (exit 1), got $rc"; exit 1; }
          grep -v '"type":"Live_Chat"' "$ref" > "$tdir/drop.md"
          rc=0; bash "$gate" --ref-file "$tdir/drop.md" >/dev/null 2>&1 || rc=$?
          [ "$rc" -eq 1 ] || { echo "✗ dropped-operation block should FAIL (exit 1), got $rc"; exit 1; }

          rm -rf "$tdir"
          echo "✓ Skill 38 TOOLS.md GHL quick-reference: source complete + concise + leak-free; installer idempotent; live gate + negatives all behave"

      - name: Skill 38 ZHC- programmatic tag-prefix rule (v1.5.0)
        shell: bash
        run: |
          set -uo pipefail
          # Machine-enforce that every tag the agent CREATES programmatically is
          # ZHC- prefixed (protocols/zhc-tag-prefix-protocol.md + MEMORY.md Rule
          # 20): the rule is documented (prefix + not-retroactive), the canonical
          # F44/F45/F47/F50 tag tokens are the ZHC- forms, and no bare create-tag
          # example literal (create_tag name= / POST .../tags "name":) survives
          # un-prefixed. BASH only (respects the .py claude-/anthropic ban).
          bash 38-conversational-ai-system/scripts/qc-zhc-tag-prefix.sh
          # Negative self-test: a planted bare create-tag example MUST FAIL (exit 1).
          ng="$(mktemp -d)"
          cp -r 38-conversational-ai-system "$ng/skill"
          printf '\nghl_skill.create_tag(location_id=<LOCATION_ID>, name="hot-lead")\n' \
            >> "$ng/skill/protocols/aggression-detection-protocol.md"
          rc=0
          bash "$ng/skill/scripts/qc-zhc-tag-prefix.sh" --skill-dir "$ng/skill" >/dev/null 2>&1 || rc=$?
          [ "$rc" -eq 1 ] || { echo "✗ ZHC- gate should FAIL (exit 1) on a planted bare create-tag, got $rc"; exit 1; }
          rm -rf "$ng"
          echo "✓ Skill 38 ZHC- tag-prefix rule documented + enforced; fails closed on a bare programmatic tag"

      - name: Skill 38 F52 JSONL data contract (v1.5.0)
        shell: bash
        run: |
          set -uo pipefail
          # Machine-enforce the F52 data contract: each of the five Round-3
          # feature logs is JSONL with timestamp + event_type, documented in its
          # protocol (path + a timestamp+event_type example line) AND in
          # INSTRUCTIONS.md (path + event_type), and seeded by
          # 25-seed-round3-feature-files.sh. BASH only.
          bash 38-conversational-ai-system/scripts/qc-feature-logs.sh
          # Negative self-test: drop a JSONL path from INSTRUCTIONS.md => FAIL (exit 1).
          ng="$(mktemp -d)"
          cp -r 38-conversational-ai-system "$ng/skill"
          grep -v 'interrupt-log.jsonl' "$ng/skill/INSTRUCTIONS.md" > "$ng/skill/INSTRUCTIONS.md.tmp"
          mv "$ng/skill/INSTRUCTIONS.md.tmp" "$ng/skill/INSTRUCTIONS.md"
          rc=0
          bash "$ng/skill/scripts/qc-feature-logs.sh" --skill-dir "$ng/skill" >/dev/null 2>&1 || rc=$?
          [ "$rc" -eq 1 ] || { echo "✗ F52 gate should FAIL (exit 1) when a JSONL path is undocumented, got $rc"; exit 1; }
          rm -rf "$ng"
          echo "✓ Skill 38 F52 data contract enforced (5 JSONL logs documented + seeded); fails closed on an undocumented log"

      - name: Skill 38 Communications Playbook Standard (mandatory checklist + negative)
        shell: bash
        run: |
          set -uo pipefail
          # Machine-enforce that references/communications-playbook-standard.md still
          # carries its hard §0 "EVERY COMMUNICATION PLAYBOOK MUST INCLUDE ALL OF THE
          # FOLLOWING" mandatory checklist and every required item: channel+persona/
          # voice, opening behavior, goal/desired-outcome, the MANDATORY SEND rule
          # (GHL Conversations API, mirror the channel, send by contactId,
          # conversationId READ-only, drafting != sending), the conversation-memory
          # read-before/append-after protocol, escalation/handoff + honesty floor,
          # quiet-hours + compliance-keyword respect, the ZHC- tag prefix for
          # programmatic tags, per-channel formatting, and the 8 channels. Mirrors the
          # workflow-ai-instructions-standard §0 rigor.
          bash 38-conversational-ai-system/scripts/qc-communications-playbook-standard.sh
          # Negative: dropping a mandatory item (the ZHC- tag prefix) MUST FAIL (exit 1).
          ng="$(mktemp -d)"; cp -r 38-conversational-ai-system "$ng/skill"
          grep -v 'ZHC-' "$ng/skill/references/communications-playbook-standard.md" > "$ng/skill/references/cps.tmp"
          mv "$ng/skill/references/cps.tmp" "$ng/skill/references/communications-playbook-standard.md"
          rc=0; bash "$ng/skill/scripts/qc-communications-playbook-standard.sh" --skill-dir "$ng/skill" >/dev/null 2>&1 || rc=$?
          [ "$rc" -eq 1 ] || { echo "✗ dropping the ZHC- tag prefix should FAIL the gate (exit 1), got $rc"; exit 1; }
          rm -rf "$ng"
          echo "✓ Skill 38 Communications Playbook Standard carries its §0 mandatory checklist; gate fails closed when a mandatory item is dropped"

      - name: Skill 38 GHL Raw Body JSON Standard (23-key FLAT rule + negative)
        shell: bash
        run: |
          set -uo pipefail
          # Machine-enforce that references/ghl-raw-body-json-standard.md documents the
          # §0 "FULL 23-key FLAT JSON" rule (23 = minimum AND standard, never fewer,
          # never nested), the FLAT rule, the placeholder-free messageTemplate stub,
          # deliver:false, and the EXACT 23 keys — and composes with qc-23-key-bodies.sh
          # so every real object-A body in the skill obeys it.
          bash 38-conversational-ai-system/scripts/qc-ghl-raw-body-standard.sh
          # Negative: dropping one of the 23 keys (location_name) MUST FAIL (exit 1).
          ng="$(mktemp -d)"; cp -r 38-conversational-ai-system "$ng/skill"
          grep -v 'location_name' "$ng/skill/references/ghl-raw-body-json-standard.md" > "$ng/skill/references/rb.tmp"
          mv "$ng/skill/references/rb.tmp" "$ng/skill/references/ghl-raw-body-json-standard.md"
          rc=0; bash "$ng/skill/scripts/qc-ghl-raw-body-standard.sh" --skill-dir "$ng/skill" >/dev/null 2>&1 || rc=$?
          [ "$rc" -eq 1 ] || { echo "✗ dropping a 23-key list entry should FAIL the gate (exit 1), got $rc"; exit 1; }
          rm -rf "$ng"
          echo "✓ Skill 38 GHL Raw Body JSON Standard documents the FLAT 23-key rule + list; gate fails closed when a key is dropped (and composes qc-23-key-bodies.sh)"

      - name: Skill 38 Notion Client-Doc Standard (ordered mandatory list + negative)
        shell: bash
        run: |
          set -uo pipefail
          # Machine-enforce that references/notion-client-doc-standard.md documents the
          # §0 "EVERY CLIENT NOTION SETUP DOC MUST INCLUDE ALL OF THE FOLLOWING, IN THIS
          # ORDER" list and its 12 items: Quick-Start first, the two-block Authorization
          # (block1 "Authorization" / block2 "Bearer <token>" value-only, NEVER a
          # combined block), the two-block Content-Type, the FLAT 23-key Raw Body,
          # tags-first + field-by-field manual fill + "Build-with-AI builds the SHAPE
          # only" warning + post-build VERIFY, the Communication Playbooks section,
          # VPS-vs-Mac, how-it-works LAST, each value in its own code block, Telegram
          # delivery, and the universal no-personal-data rule — and composes with
          # qc-reference-sheet.sh so the actually-generated doc conforms.
          bash 38-conversational-ai-system/scripts/qc-notion-doc-standard.sh
          # Negative: dropping the "Quick Start FIRST" item MUST FAIL (exit 1).
          ng="$(mktemp -d)"; cp -r 38-conversational-ai-system "$ng/skill"
          grep -v 'Quick Start FIRST' "$ng/skill/references/notion-client-doc-standard.md" > "$ng/skill/references/nd.tmp"
          mv "$ng/skill/references/nd.tmp" "$ng/skill/references/notion-client-doc-standard.md"
          rc=0; bash "$ng/skill/scripts/qc-notion-doc-standard.sh" --skill-dir "$ng/skill" >/dev/null 2>&1 || rc=$?
          [ "$rc" -eq 1 ] || { echo "✗ dropping 'Quick Start FIRST' should FAIL the gate (exit 1), got $rc"; exit 1; }
          rm -rf "$ng"
          echo "✓ Skill 38 Notion Client-Doc Standard documents the ordered mandatory list; gate fails closed when a mandatory item is dropped (and composes qc-reference-sheet.sh)"

      - name: Skill 38 ZHC Pixel (Feature 49) invariants
        shell: bash
        run: |
          set -uo pipefail
          # Machine-enforce the F49 ZHC Pixel invariants from the repo alone: the
          # pixel JS template + its three placeholders + the generator that substitutes
          # them (27-render-pixel-js.sh); the OpenClaw `pixel-visitor-signal` hook
          # (deliver:false, bot-gate-first messageTemplate, scoped Pixel Concierge agent
          # + hook:pixel: allow-list — 28-configure-pixel-hook.sh); the
          # STEP_1_45_PIXEL_CONCIERGE AGENTS.md block + protocols/zhc-pixel-protocol.md;
          # the ZHC- pixel tag prefixes (ZHC-pixel-visitor/-returning-visitor/-high-intent)
          # and ZHC_ field set (ZHC_first_visit_date/_total_visits/_pages_viewed/
          # _high_intent_signal); the GDPR/CCPA/DNT/deletion privacy controls in BOTH the
          # protocol and the template; the scope precheck (26-verify-pixel-prerequisites.sh)
          # naming Pages:Edit + Workers Scripts:Edit + Workers Routes:Edit + the token Doc;
          # and the scope-gated deploy (29-deploy-pixel-cloudflare.sh). BASH only.
          bash 38-conversational-ai-system/scripts/qc-zhc-pixel.sh
          echo "✓ Skill 38 ZHC Pixel (F49) invariants hold"

      - name: Skill 38 ZHC Pixel gate negative test (fixture)
        shell: bash
        run: |
          set -uo pipefail
          # Prove qc-zhc-pixel.sh actually FAILS when an F49 invariant is broken (a green
          # gate that can never go red is worthless). The fixture copies the skill, breaks
          # three different invariants in three throwaway copies (removes the
          # pixel-visitor-signal hook registration, drops a required ZHC_ field, neuters a
          # required scope name), and asserts the gate exits non-zero each time.
          bash 38-conversational-ai-system/scripts/qc-zhc-pixel.test.sh
          echo "✓ qc-zhc-pixel negative test passes (the gate fails on a broken invariant)"
