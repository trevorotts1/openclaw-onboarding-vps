# Install - humanizer

1. Create target folder:
   `mkdir -p ~/.openclaw/skills/humanizer`

2. Copy all files from `upstream-original/` into target.

3. Verify:
   - `~/.openclaw/skills/humanizer/SKILL.md` exists

4. Restart/reload OpenClaw skills if needed.
